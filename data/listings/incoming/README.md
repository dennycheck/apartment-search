# Drop saved Zillow/StreetEasy HTML here, then:
#   python scripts/run_hitlist_pipeline.py
#
# Or drop a JSON extract (see sample/listings_from_screenshot.sample.json).
# Screenshot images are not OCR'd by the scripts — attach them in Cursor chat instead.
